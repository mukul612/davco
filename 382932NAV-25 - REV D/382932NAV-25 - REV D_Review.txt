Review notes for 382932NAV-25 - REV D (FP382 ASM, FLUID HEAT W/ 12V PRE-HEAT)
Generated by the dimensional-layout skill. 25 balloons total (shared PDF for
both layouts). Supplier table: 22 rows (items 1-22, contiguous). Customer
table: 21 rows (items 1-19, 21, 22 -- item 20 is header-routed, see below).

CALLOUT GROUPING
- Items 10 (APTIV connector/terminal P/N, wattage rating, polarity -- 4
  printed lines) is ONE balloon / ONE Excel row per the callout-grouping
  rule, joined with "; ". Previously this had been split into 4 separate
  balloons -- corrected.

HEADER-ROUTED BALLOONS (not in the dimensional table)
- Item 20 "NAVISTAR P/N: 3594343C95" -- Customer Part Number. On the
  Customer layout it's routed to the header (Customer = NAVISTAR, Customer
  Part Number = 3594343C95), so the Customer table skips from item 19 to
  21 -- that gap is intentional. On the Supplier layout, which has no
  Customer fields at all, this same balloon automatically falls back into
  the dimensional table instead (still item 20, so the Supplier table has
  no gap).
- Item 23 "PART NO: 382932NAV-25" -- DAVCO Part Number, routed to the
  header on both layouts (Supplier N7 / Customer A7).
- Item 24 "REVISION: D" -- Revision, routed to the header on both layouts
  (Supplier X7 / Customer J7).
- Item 25 "RELEASED: 07/19/13" -- Released Date, routed to the header on
  both layouts. NEITHER template has a dedicated Released Date field, so
  it was written into the otherwise-unused "Lay-out No." box (A10) as
  "[Balloon 25] Released: 07/19/13". This is a reasonable stand-in, not a
  real labeled field -- let me know if you'd rather it go somewhere else.
- All four header-routed values are prefixed "[Balloon N]" in their Excel
  cell so they stay traceable back to the drawing.

TITLE BLOCK SCOPE (permanent rule)
- Per the title-block ballooning rule, only DAVCO Part No. (23), Revision
  (24), and Released (25) are ballooned inside the bordered title-block
  box. Title, sheet, scale, drawing size, the general tolerance block,
  third-angle projection symbol, drawn-by/checked-by/engineer/approved-by,
  company info, and original production release number are intentionally
  NOT ballooned.
- The Customer Part Number (item 20) sits outside the title-block box (in
  the drawing's general notes area), which is why it's ballooned there
  rather than inside the box.

EXCLUDED FROM BALLOONING (by design)
- This is an assembly drawing with its own List of Materials (BOM) table
  (ITEM NO. 1-21, bottom right, with its own pre-existing black circled
  reference numbers scattered across the views). Those identify assembly
  components, not inspectable dimensions, and were intentionally NOT
  re-ballooned. Confirm this is the intended scope.
- A small stray "R" character was found near the general tolerance note
  (approx. PDF coordinates x=2144, y=1377). Too small/isolated to
  confidently associate with a specific radius dimension -- not ballooned.

DUPLICATE CHECK
- "FUEL IN" and "FUEL OUT" each show a 1/2"-14 NPTF thread callout once
  (left view) and a plain identifying label without dimensions again on
  the middle view -- treated as the same port referenced twice, not a
  duplicate, so only one row each (items 5, 6).

LIMITS / TOLERANCE COLUMN (Customer template only)
- The Customer template has a separate Specification/Limits column, but
  none of this drawing's dimensions show a separable nominal + ± tolerance
  printed directly on the feature (the only real tolerance values on this
  print live in the general tolerance block, which per the title-block
  rule is not ballooned at all). The Limits column is therefore blank on
  every row -- expected, not an omission.

EVERYTHING ELSE
- All dimensions, thread/port specs, hole callouts, and notes were read
  directly from the PDF's embedded vector text (not OCR), so symbols
  (Ø, ±, °) and values should be exact.
